# Used only by explicit local batch generation. Never prefix this key with PUBLIC_.
DEEPSEEK_API_KEY=
DEEPSEEK_MODEL=deepseek-v4-flash
# PUBLIC_API_BASE_URL=http://localhost:8787
# Optional existing Chrome installation for local rendering.
# PUPPETEER_EXECUTABLE_PATH=/absolute/path/to/chrome
# PUPPETEER_CONFIG=/absolute/path/to/launch-options.json
